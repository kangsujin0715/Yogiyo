import { createTheme } from '@mui/material';

export const theme = createTheme({
	typography: {
		HtmlFontSize: '10px',
	},
  palette: {
	primary: {
		main: '#000000',
		contrastText: '#000000',
	  },
    text: {
      primary: '#000000',
    },
  },
});