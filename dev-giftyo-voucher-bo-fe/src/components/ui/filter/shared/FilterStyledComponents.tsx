import styled from 'styled-components';
import { InputLabel } from '@mui/material';

export const FilterStyledLabel = styled(InputLabel)`
  font-weight: 500;
  font-size: 14px;
  margin-bottom: 8px;
`;
